import UIKit

class Follow {

    var targetUid: String!
    var isFollow: Bool!

    init(targetUid: String, isFollow: Bool ) {
        self.targetUid = targetUid
        self.isFollow = isFollow
    }

    func save() {
        
    }
}
